# Financial Analysis Copilot Scaffolding

This directory contains the initial structure for the project.

## Structure

- `frontend/`: Frontend application code
- `middleware/`: Middleware logic
- `backend/`: Backend server code
- `database/`: Database schemas and migrations

## Deployment

Basic deployment files (`docker-compose.yml`, `Dockerfile`) are included in the root of this folder.
